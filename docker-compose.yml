version: '3.4'

services:
  postgres:
    image: postgres:12
    expose:
      - '5432'
    env_file: ./postgres.env
    volumes:
      - ./postgres_scripts:/docker-entrypoint-initdb.d/
  backend:
    build:
      context: ./backend
      target: development
    image: backend:dev
    ports:
      - '3000:3000'
    env_file: ./backend.env
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
  frontend:
    build:
      context: ./frontend
      target: development
    image: frontend:dev
    ports: 
      - '8080:8080'
    volumes:
      - ./frontend:/app
    depends_on:
      - backend

